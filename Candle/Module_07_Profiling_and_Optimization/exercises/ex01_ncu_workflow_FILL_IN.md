# Fill In: Nsight Compute Workflow

1. Run a baseline profile command:
   `_____`

2. Capture tensor core metric:
   `_____`

3. Capture dram throughput metric:
   `_____`

4. Write your findings:
- Top 3 kernels by time:
- Is workload compute or memory bound?
- First optimization to try:
